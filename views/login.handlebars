<div class="jumbotron jumbotron-fluid">
    <h1 class="display-4"><a href="/">CarTech</a></h1>
    <p class="lead">Please Login to sell your car</p>
    <hr class="my-4">
</div>

<div id="home">
    <div class="container">
        <div class="row">
            <div class="col-8 offset-2">
                <form class="clearfix mb-4" action="POST">
                    <div class="form-group">
                        <h3>Visitor:</h3>
                        <label for="cars-text">What is your Name</label>
                        <input type="text" id="login" class="form-control" aria-describedby="login-text" placeholder="Enter name here">
                        <button id="submit-login" class="btn btn-primary float-right">Submit</button>
                        <ul id="login-list" class="list-group">
                            {{#each customer}}
                            <li data-id="{{this.id}}" class="list-login-item">
                                <a href="login/{{this.id}}"></a>
                                <button class="btn btn-danger float-right delete">x</button>
                            </li>
                            {{/each}}
                        </ul>
                    </div>
            </div>
        </div>
    </div>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="/js/index.js"></script>